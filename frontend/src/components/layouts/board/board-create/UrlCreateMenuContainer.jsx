import { CreateMenuContainerStyle } from './BoardCreate.styles';

const UrlCreateMenuContainer = ({ children }) => {
  return <CreateMenuContainerStyle>{children}</CreateMenuContainerStyle>;
};

export default UrlCreateMenuContainer;
