import { BoardPostContentWrapperStyle } from './MyBoard.styles';

const BoardPostContentWrapper = ({ children }) => {
  return <BoardPostContentWrapperStyle>{children}</BoardPostContentWrapperStyle>;
};

export default BoardPostContentWrapper;
