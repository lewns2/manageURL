import { BoardUrlWrapperStyle } from './MyUrl.styles';

const BoardUrlWrapper = ({ children }) => {
  return <BoardUrlWrapperStyle>{children}</BoardUrlWrapperStyle>;
};

export default BoardUrlWrapper;
